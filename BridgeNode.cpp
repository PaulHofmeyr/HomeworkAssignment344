#include "BridgeNode.h"
#include "NodeUtils.h"

// GLB bridges from objects-map.csv — flat brown bridge planks removed.
std::shared_ptr<SceneNode> buildBridgeNode(CourseLayout&)
{
    return std::make_shared<SceneNode>();
}
